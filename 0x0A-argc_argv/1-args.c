#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints the number of arguments
 * @argc: argument count
 * @argv: argument vector array
 * auth - gbotemi
 * Return 0 always
 */

int main(int argc, char *argv[])
{
(void) argv;
printf("%d\n", argc - 1);

return (0);
}
