#include "main.h"

/**
 * _abs - computes the absolute value of a whole number (integer)
 * @n: the value to check
 * Return: the absolute value of int
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
